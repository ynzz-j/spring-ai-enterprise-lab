# requirement-to-testcase package

这里后续放具体工作流节点。

建议类：

- `RequirementExtractNode`
- `ApiDesignNode`
- `TestCaseGenerateNode`
- `ReleaseChecklistNode`
- `WorkflowReportAssembler`

设计约束：

- 每个节点职责单一。
- AI 输出结构化。
- 报告必须列出不确定项和人工确认记录。

