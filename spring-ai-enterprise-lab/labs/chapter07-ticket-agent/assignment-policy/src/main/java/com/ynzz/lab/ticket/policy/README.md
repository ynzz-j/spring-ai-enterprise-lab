# assignment-policy package

这里后续放派单策略代码。

建议类：

- `SkillProfile`
- `OnDutySchedule`
- `AssignmentPolicy`
- `AssigneeScore`
- `AssignmentReason`

设计约束：

- 推荐依据包括技能、排班、当前负载、SLA 风险。
- 策略结果要可解释，不能只返回一个人名。

